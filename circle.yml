machine:
  python:
    version: 3.5.1

dependencies:
  override:
    - pip install --upgrade pip
    - pip install -r requirements.txt
    - pip install -U tox
    - pip --version
    - tox --version

test:
  override:
    - tox