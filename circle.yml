machine:
  python:
    version: 3.5.1

dependencies:
  pre:
    - pip install --upgrade pip
    - pip install -U tox
    - pip --version
    - tox --version
  override:
    - pip install -r requirements.txt

test:
  override:
    - tox