sudo: false

language: python
python:
    - "3.5"

os:
    - linux

# command to install dependencies
install:
    - pip install --upgrade pip
    - pip install -U tox
    - pip --version
    - tox --version

# command to run tests
script: tox