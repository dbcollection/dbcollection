classdef dbcollection_utils_pad
    % Array padding functions.

    properties
    end

    methods
        function ascii = pad_list(obj, input)
            % Convert a string to an array

            %% TODO
        end

        function str = unpad_list(obj, input)
            % convert an array/matrix to an array of strings

            %% TODO
        end
    end

end
